import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  icon: {
    position: 'absolute',
    top: -20,
    left: 12,
    width: 40,
    height: 40,
    zIndex: 1,
  },
  inner: {
    paddingTop: 50,
  },
});
